# Plan: 7 Nuevas Features para Business Opportunity Agent

## Resumen
Expandir el MVP actual con 7 features nuevas: Multi-Industry, Outreach Tracking, Email Sending, Dashboard Analytics, AI Conversation, PDF Export, y Webhooks. **NO incluye** custom scoring rules (usuario lo excluyó explícitamente).

**Stack:** Next.js 16 + React 19 + Tailwind v4 + Supabase + Vercel AI SDK
**UI:** shadcn/ui + recharts + @tanstack/react-table (ya instalados)
**Idioma UI y proposals:** Español

---

## Orden de Implementación (por dependencias)

| # | Feature | Dependencias | Prioridad |
|---|---------|--------------|-----------|
| 1 | Multi-Industry | Ninguna (foundational) | Alta |
| 2 | Outreach Tracking | Multi-Industry | Alta |
| 3 | Email Sending | Multi-Industry | Alta |
| 4 | Dashboard Analytics | Multi-Industry | Media |
| 5 | AI Conversation | Multi-Industry, Outreach Tracking | Media |
| 6 | PDF Export | Multi-Industry, Outreach Tracking | Baja |
| 7 | Webhooks | Multi-Industry | Baja |

---

## Feature 1: Multi-Industry Support

**Objetivo:** Permitir al usuario definir industrias (dental, restaurantes, etc.) con prompts y queries personalizadas.

### Archivos a crear/modificar

**Nueva tabla SQL:**
```sql
-- supabase/migrations/002_multi_industry.sql
CREATE TYPE industry_status AS ENUM ('active', 'archived');

CREATE TABLE industries (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  name TEXT NOT NULL,
  slug TEXT NOT NULL,
  icon TEXT, -- emoji or lucide icon name
  search_queries JSONB NOT NULL DEFAULT '[]', -- ["dentistas en Valencia", "clínicas dentales Valencia"]
  prospecting_prompt TEXT, -- custom AI prompt for this industry
  research_prompt TEXT,
  proposal_template TEXT, -- custom proposal template
  scoring_weights JSONB, -- optional custom weights override
  status industry_status NOT NULL DEFAULT 'active',
  leads_count INTEGER DEFAULT 0,
  avg_score DECIMAL(5,1),
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(user_id, slug)
);
```

**Modificar `leads` table:**
- Agregar columna `industry_id UUID REFERENCES industries(id)`
- Agregar índice `idx_leads_industry_id`

**Nuevos archivos:**
- `src/lib/agents/industries.ts` — CRUD functions for industries
- `src/app/api/industries/route.ts` — GET (list) + POST (create)
- `src/app/api/industries/[id]/route.ts` — GET + PATCH + DELETE
- `src/app/(dashboard)/industries/page.tsx` — Industries list page (Server Component)
- `src/app/(dashboard)/industries/IndustriesClient.tsx` — Client component for CRUD UI
- `src/components/features/dashboard/IndustryCard.tsx` — Industry card component
- `src/components/features/dashboard/IndustryForm.tsx` — Create/edit form

**Modificar:**
- `src/types/index.ts` — Agregar `Industry` interface, `industry_id` a `Lead`
- `src/lib/agents/pipeline.ts` — Accept `industryId` parameter, use industry-specific prompts
- `src/lib/agents/prospector.ts` — Accept `searchQueries` from industry config
- `src/lib/agents/scorer.ts` — Accept optional `scoringWeights` from industry config
- `src/lib/agents/proposer.ts` — Accept optional `proposalTemplate` from industry config
- `src/components/features/dashboard/PipelineControl.tsx` — Industry selector dropdown
- `src/components/features/dashboard/LeadsTable.tsx` — Industry column/filter
- `src/components/features/dashboard/StatsCards.tsx` — Industry-filtered stats
- `src/components/features/dashboard/StatusBadge.tsx` — Add industry context
- `src/app/(dashboard)/dashboard/page.tsx` — Pass industry context
- `src/app/(dashboard)/leads/page.tsx` — Pass industry context

**Sidebar navigation:**
- `src/app/(dashboard)/layout.tsx` — Add "Industrias" nav item

---

## Feature 2: Outreach Tracking

**Objetivo:** Trackear intentos de contacto (llamadas, emails, WhatsApp) con historial completo por lead.

### Archivos a crear/modificar

**Modificar `contact_attempts` table (ya existe):**
```sql
-- supabase/migrations/003_outreach_tracking.sql
-- Enhance contact_attempts table
ALTER TABLE contact_attempts
  ADD COLUMN user_id UUID REFERENCES auth.users(id),
  ADD COLUMN contact_type TEXT NOT NULL DEFAULT 'email', -- email, phone, whatsapp, linkedin, other
  ADD COLUMN status TEXT NOT NULL DEFAULT 'pending', -- pending, sent, delivered, opened, replied, bounced
  ADD COLUMN sentiment TEXT, -- positive, neutral, negative
  ADD COLUMN response_received BOOLEAN DEFAULT false,
  ADD COLUMN response_summary TEXT,
  ADD COLUMN next_action TEXT,
  ADD COLUMN next_action_date DATE,
  ADD COLUMN attachments JSONB DEFAULT '[]',
  ADD COLUMN metadata JSONB DEFAULT '{}';

-- Add lead outreach summary view
CREATE VIEW lead_outreach_summary AS
SELECT
  l.id as lead_id,
  l.name as lead_name,
  COUNT(ca.id) as total_attempts,
  COUNT(ca.id) FILTER (WHERE ca.response_received = true) as responses_received,
  MAX(ca.created_at) as last_contact_at,
  MIN(ca.created_at) as first_contact_at,
  ca_outcome.most_recent_outcome
FROM leads l
LEFT JOIN contact_attempts ca ON ca.lead_id = l.id
LEFT JOIN (
  SELECT DISTINCT ON (lead_id) lead_id, outcome as most_recent_outcome
  FROM contact_attempts ORDER BY lead_id, created_at DESC
) ca_outcome ON ca_outcome.lead_id = l.id
GROUP BY l.id, l.name, ca_outcome.most_recent_outcome;
```

**Nuevos archivos:**
- `src/lib/agents/outreach.ts` — CRUD functions for contact attempts
- `src/app/api/leads/[id]/contacts/route.ts` — GET (list) + POST (create contact attempt)
- `src/app/api/leads/[id]/contacts/[contactId]/route.ts` — PATCH + DELETE
- `src/components/features/dashboard/OutreachTimeline.tsx` — Visual timeline of contacts
- `src/components/features/dashboard/ContactAttemptForm.tsx` — Form to log new contact
- `src/components/features/dashboard/ContactStatusBadge.tsx` — Status badge for contacts

**Modificar:**
- `src/types/index.ts` — Add `ContactAttempt` interface with new fields, add `outreachStatus` to `Lead`
- `src/app/(dashboard)/leads/[id]/page.tsx` — Add outreach timeline section
- `src/components/features/dashboard/StatusBadge.tsx` — Add outreach-related statuses

---

## Feature 3: Email Sending

**Objetivo:** Enviar emails directamente desde la plataforma usando Resend o SendGrid.

### Stack de Email
- **Proveedor:** Resend (simple, moderno, API-first)
- **Límite free tier:** 100 emails/día, 3,000/mes
- **Alternativa:** SendGrid (más features, mismo precio)

### Archivos a crear/modificar

**Nuevos archivos:**
- `src/lib/email/client.ts` — Resend/SendGrid client wrapper
- `src/lib/email/templates/proposal.ts` — HTML email template for proposals
- `src/lib/email/templates/followup.ts` — Follow-up email template
- `src/app/api/leads/[id]/send/route.ts` — POST — send email via provider
- `src/components/features/dashboard/SendEmailDialog.tsx` — Dialog to preview + send email
- `src/components/features/dashboard/EmailPreview.tsx` — Email preview component

**Modificar:**
- `src/types/index.ts` — Add `EmailSendResult` interface
- `src/app/(dashboard)/leads/[id]/page.tsx` — Add "Send Email" button
- `src/components/features/dashboard/ProposalView.tsx` — Add "Send" action button
- `.env.local` — Add `RESEND_API_KEY` variable
- `supabase/migrations/003_outreach_tracking.sql` — Update `contact_attempts` to auto-log sent emails

**Variables de entorno nuevas:**
```
RESEND_API_KEY=re_...
EMAIL_FROM=Valencia Studio <noreply@valenciastudio.es>
```

---

## Feature 4: Dashboard Analytics

**Objetivo:** Gráficos e insights: leads por industria, pipeline funnel, scoring distribution, outreach metrics.

### Archivos a crear/modificar

**Nuevos archivos:**
- `src/app/(dashboard)/analytics/page.tsx` — Analytics page (Server Component)
- `src/app/(dashboard)/analytics/AnalyticsClient.tsx` — Client component with charts
- `src/components/features/dashboard/charts/PipelineFunnel.tsx` — Funnel chart (recharts)
- `src/components/features/dashboard/charts/ScoreDistribution.tsx` — Histogram chart
- `src/components/features/dashboard/charts/IndustryBreakdown.tsx` — Pie chart by industry
- `src/components/features/dashboard/charts/OutreachMetrics.tsx` — Line chart (contacts over time)
- `src/components/features/dashboard/charts/ConversionRate.tsx` — Conversion metrics

**Modificar:**
- `src/app/(dashboard)/layout.tsx` — Add "Analytics" nav item
- `src/types/index.ts` — Add analytics-related types

**Nuevas vistas SQL:**
```sql
-- supabase/migrations/004_analytics.sql
CREATE VIEW analytics_pipeline_funnel AS
SELECT
  status,
  COUNT(*) as count,
  ROUND(COUNT(*)::decimal / NULLIF(SUM(COUNT(*)) OVER (), 0) * 100, 1) as percentage
FROM leads
GROUP BY status
ORDER BY
  CASE status
    WHEN 'discovered' THEN 1
    WHEN 'researched' THEN 2
    WHEN 'audited' THEN 3
    WHEN 'scored' THEN 4
    WHEN 'proposal_ready' THEN 5
    WHEN 'contacted' THEN 6
    WHEN 'qualified' THEN 7
    WHEN 'converted' THEN 8
    WHEN 'rejected' THEN 9
  END;

CREATE VIEW analytics_score_distribution AS
SELECT
  CASE
    WHEN opportunity_score >= 90 THEN '90-100 (Excelente)'
    WHEN opportunity_score >= 80 THEN '80-89 (Muy Bueno)'
    WHEN opportunity_score >= 70 THEN '70-79 (Bueno)'
    WHEN opportunity_score >= 60 THEN '60-69 (Regular)'
    WHEN opportunity_score >= 50 THEN '50-59 (Bajo)'
    ELSE '0-49 (Muy Bajo)'
  END as range,
  COUNT(*) as count
FROM leads
WHERE opportunity_score IS NOT NULL
GROUP BY range
ORDER BY MIN(opportunity_score) DESC;

CREATE VIEW analytics_industry_stats AS
SELECT
  i.name as industry_name,
  i.slug as industry_slug,
  COUNT(l.id) as total_leads,
  ROUND(AVG(l.opportunity_score), 1) as avg_score,
  COUNT(l.id) FILTER (WHERE l.status = 'converted') as converted_count,
  COUNT(l.id) FILTER (WHERE l.opportunity_score >= 80) as high_priority_count
FROM industries i
LEFT JOIN leads l ON l.industry_id = i.id
GROUP BY i.id, i.name, i.slug;
```

**Lib de analytics:**
- `src/lib/analytics/queries.ts` — Supabase queries for chart data

---

## Feature 5: AI Conversation (Chat)

**Objetivo:** Chat interactivo con IA para consultar leads, pedir recomendaciones, estrategias de outreach, etc.

### Stack
- **UI:** shadcn/ui Chat components o stepper personalizado
- **AI:** Vercel AI SDK `streamText()` con herramientas (tools)

### Archivos a crear/modificar

**Nuevos archivos:**
- `src/app/(dashboard)/chat/page.tsx` — Chat page (Server Component)
- `src/app/(dashboard)/chat/ChatClient.tsx` — Chat UI with streaming
- `src/app/api/chat/route.ts` — POST — AI chat endpoint with tools
- `src/lib/ai/chat-tools.ts` — AI tools definitions (query leads, get lead details, get analytics, suggest outreach)
- `src/components/features/dashboard/ChatMessage.tsx` — Message bubble component
- `src/components/features/dashboard/ChatInput.tsx` — Input component
- `src/components/features/dashboard/ChatToolResult.tsx` — Tool call result display

**Modificar:**
- `src/app/(dashboard)/layout.tsx` — Add "Chat" nav item
- `src/types/index.ts` — Add chat-related types

**AI Tools disponibles:**
```typescript
// Tools que la IA puede usar al responder
tools: {
  getLeadDetails: async (leadId) => { /* fetch lead + audit + proposal */ },
  searchLeads: async (query, filters) => { /* search leads */ },
  getAnalytics: async (period) => { /* get analytics data */ },
  suggestOutreach: async (leadId) => { /* suggest outreach strategy */ },
  getTopOpportunities: async (limit) => { /* get top leads */ },
}
```

---

## Feature 6: PDF Export

**Objetivo:** Exportar propuestas y reportes de leads como PDF profesional.

### Stack
- **PDF:** `@react-pdf/renderer` (React components → PDF) o `jspdf` + `html2canvas`
- **Preferencia:** `@react-pdf/renderer` — más profesional, mejor control de layout

### Archivos a crear/modificar

**Nuevos archivos:**
- `src/lib/pdf/ProposalDocument.tsx` — React-PDF proposal document component
- `src/lib/pdf/LeadReportDocument.tsx` — React-PDF lead report document
- `src/lib/pdf/generate.ts` — PDF generation helper
- `src/app/api/leads/[id]/proposal/[proposalId]/pdf/route.ts` — GET — generate + download PDF
- `src/app/api/leads/[id]/report/route.ts` — GET — generate lead report PDF
- `src/components/features/dashboard/ExportPdfButton.tsx` — Button component for PDF export

**Modificar:**
- `src/app/(dashboard)/leads/[id]/page.tsx` — Add "Export PDF" button
- `src/components/features/dashboard/ProposalView.tsx` — Add "Download PDF" button
- `package.json` — Add `@react-pdf/renderer` dependency

**Instalar:**
```bash
npm install @react-pdf/renderer
```

---

## Feature 7: Webhooks

**Objetivo:** Notificar a servicios externos (Slack, Zapier, n8n) cuando hay eventos (lead scored, proposal ready, etc.).

### Archivos a crear/modificar

**Nueva tabla SQL:**
```sql
-- supabase/migrations/005_webhooks.sql
CREATE TYPE webhook_event AS ENUM (
  'lead.created',
  'lead.scored',
  'lead.audited',
  'proposal.created',
  'proposal.sent',
  'contact.created',
  'pipeline.completed'
);

CREATE TABLE webhooks (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  name TEXT NOT NULL,
  url TEXT NOT NULL,
  events webhook_event[] NOT NULL DEFAULT '{}',
  secret TEXT, -- for HMAC verification
  is_active BOOLEAN DEFAULT true,
  last_triggered_at TIMESTAMPTZ,
  last_response_code INTEGER,
  failure_count INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE webhook_deliveries (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  webhook_id UUID NOT NULL REFERENCES webhooks(id) ON DELETE CASCADE,
  event webhook_event NOT NULL,
  payload JSONB NOT NULL,
  response_code INTEGER,
  response_body TEXT,
  duration_ms INTEGER,
  success BOOLEAN NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

**Nuevos archivos:**
- `src/lib/webhooks/dispatcher.ts` — Webhook dispatcher (queue + retry logic)
- `src/lib/webhooks/payloads.ts` — Payload formatters per event type
- `src/app/api/webhooks/route.ts` — GET (list) + POST (create)
- `src/app/api/webhooks/[id]/route.ts` — GET + PATCH + DELETE
- `src/app/api/webhooks/[id]/test/route.ts` — POST — test webhook
- `src/app/(dashboard)/webhooks/page.tsx` — Webhooks management page
- `src/app/(dashboard)/webhooks/WebhooksClient.tsx` — Client component for CRUD
- `src/components/features/dashboard/WebhookCard.tsx` — Webhook card with status
- `src/components/features/dashboard/WebhookForm.tsx` — Create/edit webhook form
- `src/components/features/dashboard/WebhookDeliveryLog.tsx` — Delivery history table

**Modificar:**
- `src/types/index.ts` — Add `Webhook`, `WebhookDelivery` interfaces
- `src/app/(dashboard)/layout.tsx` — Add "Webhooks" nav item
- `src/lib/agents/pipeline.ts` — Dispatch events after pipeline steps complete
- `src/lib/agents/scorer.ts` — Dispatch `lead.scored` event
- `src/lib/agents/auditor.ts` — Dispatch `lead.audited` event
- `src/lib/agents/proposer.ts` — Dispatch `proposal.created` event

**Dispatcher implementation:**
```typescript
// src/lib/webhooks/dispatcher.ts
import crypto from 'crypto';

export async function dispatchWebhook(
  event: WebhookEvent,
  payload: Record<string, unknown>,
  userId: string
) {
  // 1. Find active webhooks for this user + event
  // 2. For each webhook:
  //    a. Generate HMAC signature with webhook secret
  //    b. POST to webhook URL with payload
  //    c. Log delivery in webhook_deliveries
  //    d. Update last_triggered_at, failure_count
  // 3. Use queue (Inngest or simple Supabase-based) for retries
}
```

---

## Sprint Breakdown

### Sprint 1: Multi-Industry (Days 1-2)
- [ ] Create `industries` table migration
- [ ] Create industry CRUD API routes
- [ ] Create industries page UI
- [ ] Modify pipeline to accept industry context
- [ ] Update LeadsTable with industry column
- [ ] Update sidebar navigation

### Sprint 2: Outreach + Email (Days 3-4)
- [ ] Enhance `contact_attempts` table
- [ ] Create outreach tracking API routes
- [ ] Create OutreachTimeline component
- [ ] Create outreach page for leads
- [ ] Set up Resend client
- [ ] Create email templates
- [ ] Create send email dialog
- [ ] Integrate send action into proposal view

### Sprint 3: Analytics + Chat (Days 5-6)
- [ ] Create analytics SQL views
- [ ] Create analytics queries lib
- [ ] Create analytics page with recharts
- [ ] Create 4 chart components
- [ ] Set up AI chat endpoint with tools
- [ ] Create chat UI components
- [ ] Create chat page

### Sprint 4: PDF + Webhooks (Days 7-8)
- [ ] Install @react-pdf/renderer
- [ ] Create proposal PDF document
- [ ] Create lead report PDF document
- [ ] Create PDF API routes
- [ ] Create webhooks table migration
- [ ] Create webhook CRUD API routes
- [ ] Create webhook dispatcher
- [ ] Create webhooks management page
- [ ] Integrate webhook dispatchers in agents

---

## Verificación

### Para cada feature:
1. `npm run lint` — Sin errores
2. `npx tsc --noEmit` — Sin errores TypeScript
3. `npm run build` — Build exitoso
4. `npm run dev` — App funciona, UI sin errores
5. **Supabase Dashboard** — Tablas creadas correctamente
6. **Browser** — CRUD operations funcionan, datos persisten

### Para email:
- Configurar `RESEND_API_KEY` en `.env.local`
- Enviar email de prueba desde UI
- Verificar en Resend dashboard que el email fue enviado

### Para webhooks:
- Crear webhook en UI con URL de prueba (webhook.site)
- Ejecutar pipeline o crear lead
- Verificar que webhook.site recibe el payload

### Para PDF:
- Generar PDF de propuesta
- Verificar que descarga correctamente
- Verificar que el PDF tiene formato profesional

### Para chat:
- Abrir chat page
- Enviar mensaje de prueba
- Verificar que la IA responde con streaming
- Verificar que la IA puede consultar leads y analytics
